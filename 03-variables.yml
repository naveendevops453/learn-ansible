- name: Variables
  hosts: localhost
  vars:
    URL: play.example.com
  tasks:
    - name: Print URL
      ansible.builtin.debug:
        msg: URL - {{ URL }}

    - name: Print fruit name
      ansible.builtin.debug:
        msg: FRUIT - {{ FRUIT }}
      vars:
        FRUIT: apple